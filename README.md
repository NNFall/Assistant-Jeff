# Assistant Jeff 0.6

Голосовой помощник для Windows. Electron-приложение объединяет локальное ожидание **Hey Jarvis**, потоковое распознавание **Gemini 3.5 Transcribe Live**, агент **AgentCommands** с вызовами функций Gemini, заметки, напоминания, инструменты управления Windows и локальный голос **Денис**.

Версия 0.6 собрана и установлена на рабочем компьютере 20 сентября 2026 года. Основной интерфейс разделён на **«Помощник»**, **«История»** и **«Настройки»**; технические сведения открываются по кнопке **«Подробнее»**.

`npm start` открывает этот общий интерфейс. Старый интерфейс 0.2 сохранён отдельно: `npm run start:legacy`. Белое тестовое окно осталось только для регрессионных проверок.

## Использование

На вкладке **«Помощник»** введите команду и нажмите **«Выполнить»** или **Ctrl+Enter**. Кнопки примеров заполняют поле, но сами ничего не выполняют. Агент выбирает только разрешённые инструменты и перечитывает состояние перед зависимым действием:

- «Найди заметку про хлеб и измени её на “купить хлеб и молоко”» — чтение, проверка ожидаемого текста, изменение и повторная проверка.
- «Покажи напоминания про чай» или «Удали напоминание, если оно всё ещё про чай в 18:30» — поиск и изменение данных с проверкой текущих полей.
- «Напомни завтра в 18:30 проверить чай» — расчёт срока в локальной зоне; неполное время становится уточнением.
- «Открой поддерживаемое приложение» или «Сверни Google Chrome» — работа с существующим окном или приложением.
- «Выбери пункт в окне» — дополнительный выбор Jev среди уже найденных вариантов; сам выбор ничего не выполняет.
- «Почему небо голубое?» — ответ Gemini; Windows при этом не изменяется.

Jev может дополнительно помочь выбрать неоднозначное действие среди уже найденных вариантов; он не создаёт селекторы, команды оболочки или аргументы. Общий маршрут использует вызовы функций Gemini через шлюз, а Windows-код выполняется только локальными разрешёнными инструментами. Полное управление любым приложением и любым UIA-провайдером не гарантируется.

Если сказать «Напомни завтра проверить чай» без времени, Jeff попросит указать точное время. Ответ можно дать в том же разговоре, например `18:30` или `в 18:30`; перед сохранением срок будет проверен. Контекст разговора хранится ограниченное время — до 30 минут — и очищается кнопкой **«Очистить контекст»** или остановкой задачи. Если точного времени нет, Jeff попросит уточнить его, а сам срок не придумает.

Для голоса нажмите **«Сказать команду»** для одной записи либо **«Включить Jarvis»** и произнесите **«Hey Jarvis»**. Микрофон выключен при запуске; статус виден рядом с кнопками и внизу окна. Имя определяется локально. После активации PCM немедленно буферизуется, а потоковое соединение может подключаться параллельно; ограниченный буфер составляет примерно **2 секунды**. Промежуточный текст отображается, но не запускает действия. Через **2 секунды тишины** запись завершается; только финальный текст передаётся AgentCommands. Кнопка **«Завершить запись»** завершает фразу раньше. В **«Настройки → Распознавание речи»** можно выбрать резервный режим **«После записи»** с MP3.

Сигнал активации, ответ Дениса и автоматическое выполнение после распознавания включаются отдельно во вкладке **«Настройки»**. Если автоматическое выполнение выключено, микрофон останавливается, а текст остаётся в поле для проверки. Поправьте его и нажмите **«Выполнить»**. Денис работает локально через Piper и озвучивает результат голосовых и текстовых задач. Озвучка текстовой задачи сама микрофон не включает.

Во время задачи карточка показывает текущий шаг и прошедшее время. **«Остановить»** прекращает дальнейшую обработку и озвучку; уже выполненные действия не отменяются. Результат остаётся на экране, включая понятную причину неудачи и предложение, что сделать дальше. Частичное выполнение и неподтверждённое действие показываются отдельно от успеха. Ошибка голосового ответа не означает, что сама задача не выполнена: результат можно прочитать.

Напоминание появляется отдельной карточкой и уведомлением Windows, если уведомления доступны. Закрытие карточки отмечает напоминание доставленным. Закрытие основного окна оставляет приложение в трее; полный выход — через меню трея. Подробная инструкция по интерфейсу: [версия 0.4](docs/friendly-ui-v04.md).

## Возможности и ограничения

`AgentCommands` ведёт цикл «наблюдение → один вызов → результат инструмента → повторное наблюдение». Вызовы с эффектом не повторяются автоматически; устаревшее или неопределённое состояние требует новой проверки. Финальный `assistant_respond` может назвать задачу завершённой только с идентификаторами проверенных результатов.

- Инструменты данных поддерживают поиск, чтение, создание, изменение и удаление заметок; для напоминаний также создание, изменение, удаление и завершение. Изменение требует свежей цели и ожидаемых полей (`expectedText`, для напоминания также `expectedDueAt`), после чего Store перечитывается.
- `clock_now` и `time_resolve` работают с явным локальным временем и ISO со смещением; недостающий час или день не угадывается.
- Существующий помощник FlaUI/C# остаётся системным путём для окон, списка мониторов, запуска приложений из разрешённого списка и запасных операций. Дополнительный адаптер Microsoft `winappCli v0.6.1` даёт более широкое дерево и поиск элементов UIA, а также фиксированные операции `select`, `toggle`, `expand`, `scroll`, `invoke` без Python и произвольной командной строки. Это возможность в текущей разработке, а не гарантия управления любым Windows-приложением.
- `windows_text_fields` уже читает обычные доступные для записи поля `ValuePattern` в одном свежем наблюдаемом неминимизированном окне; запрошенные обычные значения могут попасть в контекст модели как результат инструмента. Поля пароля, секретные значения и чувствительный контекст исключаются, фокус для чтения и замены не требуется. `windows_text_replace` заменяет всё поле через UI Automation без нажатия клавиш и считает действие подтверждённым только после точного повторного чтения и проверки хеша. Цель привязана к процессу, окну, идентификатору UI Automation и предыдущему значению; устаревшее или изменившееся состояние останавливает замену. Произвольные команды оболочки, PowerShell, операции с процессами, реестром, файлами, буфером обмена, координатами и придуманные сочетания клавиш запрещены.
- Подробный отчёт разделяет доступные возможности, полноту наблюдения, решения, результаты инструментов и финальный ответ. В голосовом режиме промежуточная транскрипция служит только для показа; Denis озвучивает только итог.

## Данные

SQLite и защищённые настройки провайдеров остаются в `%APPDATA%\Assistant Jeff`. Используется прежний `Store` и совместимая схема заметок/напоминаний. Существующая база не заменяется; если её ещё нет, исходная `data\assistant.sqlite` копируется через SQLite без изменения оригинала. Настройки нового голосового режима хранятся отдельно в `windows-voice-settings.json`.

Во вкладке **«История»** показаны последние 20 задач. Выберите запись для полного отчёта или нажмите **«Папка журналов»**. Подробный отчёт показывает доступные возможности, полноту наблюдения, решения, результаты инструментов и финальные ответы; аудиобайты и промежуточные гипотезы распознавания не записываются. Контекст разговора живёт в памяти и очищается отдельной кнопкой; в ожидании локального сигнала активации облачной трансляции микрофона нет.

Ключи защищены Windows DPAPI текущего пользователя и не включаются в Git или установщик. SQLite с заметками и напоминаниями не зашифрована. В «Настройках» видна доступность компонентов; это проверка локальной настройки и ресурсов, а не гарантия ответа облачного сервиса. Провайдеры требуют настроенного доступа; полного мастера настройки аккаунтов пока нет.

## Разработка и сборка

```powershell
npm.cmd ci
npm.cmd run windows:build
npm.cmd run prepare:runtime
npm.cmd start
npm.cmd test
npm.cmd run pack
npm.cmd run build
```

Перед запуском нужны ONNX-модели сигнала активации, модель Denis, помощник Windows, Piper и FFmpeg. Для расширенного управления UIA используется закреплённый исполняемый компонент Windows `winappCli v0.6.1`; его подготовка описана в `scripts/prepare-winapp-runtime.ps1` и запускается из `prepare:runtime`. Это предварительная возможность: наличие подготовленного exe не означает полную совместимость со всеми Windows-приложениями. Подробности: [голосовой runtime 0.3](docs/voice-windows-v03.md) и [исследование Windows framework/runtime](docs/windows-frameworks-2026-09-20.md).

`pack` создаёт распакованную Windows x64-сборку, `build` — NSIS-установщик в `release`. Секреты, базы и журналы в пакет не входят. Версия исходников и созданный установщик не означают, что пользователь уже установил обновление.

## Проверка

Проверки 0.6: **631 тест пройден, 1 пропущен**, 10 сценариев настоящего Electron-интерфейса с подставными внешними сервисами, 45 проверок native-контракта и 16 действий в собственном Windows-окне. Реальный Gemini прошёл 8 последовательных задач с заметками и напоминаниями в отдельной тестовой базе. Проверены даты «завтра» и «послезавтра», изменение по контексту и отсутствие повторной записи после исправления ответа модели. Полные задачи заняли 4,2–9,0 с, включая восстановление после одного upstream 503; это отдельная небольшая выборка.

На настоящем рабочем столе Google Chrome восстановлен из свёрнутого состояния и проверен как активное окно; запрос занял 11,8 с. Громкость прочитана через CoreAudio. Восстановление уже открытого Диспетчера задач не прошло — его окно осталось свёрнутым. Read-only проверка токенов установила разные уровни прав: Task Manager работает с high integrity, помощник — с medium. Привилегии автоматически не повышались; управление такими окнами не заявляется. Winapp select и invoke→observe прошли три последовательных прогона по 7 проверок после одного необъяснённого сбоя чтения результата; toggle/expand/scroll здесь покрыты подставными тестами, но не этим native-стендом.

Синтетическое аудио прошло через Gemini Live → AgentCommands → одно напоминание. Финал транскрипции получен через 280 мс после завершения передачи, подключение заняло 2,87 с, исполнение после распознавания — 3,43 с. Физический микрофон и качество эхоподавления в этой проверке не использовались. Установленный пакет сопоставлен с исходниками и ресурсами; обе личные базы сохранили прежние хеши. Ключи и тестовые базы не входят в Git или установщик.

Автоматические проверки запускаются через `npm test`. Они включают обработку состояний и результатов, голосовой цикл, отмену и работу реестра разрешённых инструментов с подставными зависимостями. Голосовые API-прогоны используют синтетическое аудио; это не проверка микрофона пользователя. Проверка сборки, локального компонента и управления конкретным приложением оцениваются отдельно. Актуальная схема: [архитектура 0.6](docs/architecture.md), [агентская система 0.6](docs/agent-system-v06.md); исследования и границы измерений: [Jev, 20.09.2026](docs/jev-architecture-research-2026-09-20.md), [восемь постов и исходники, 21.09.2026](docs/jev-x-research-2026-09-21.md), [эксперимент выбора Windows-действий](docs/jev-grounding-evaluation-2026-09-21.md).

Исторические измерения версии 0.3: транскрипция MP3 Gemini — **1 966 мс**, разговорный `/chat` — **2 812 мс**. Это время отдельных API-вызовов, не задержка всей голосовой команды и не тест микрофона пользователя. Синтетическая проверка вызова функции **1 199 мс** — один вызов модели и инструмента, а не полная задача с несколькими шагами и повторным чтением состояния Windows. Сквозное произнесение имени, реальный микрофон, AEC и поведение в шуме отдельно не подтверждены.

Устройство Windows-цикла и ограничения: [управление Windows](docs/windows-desktop.md). Исторические руководства по голосу и упаковке: [версия 0.3](docs/voice-windows-v03.md), по светлому интерфейсу: [версия 0.4](docs/friendly-ui-v04.md). Старый изолированный стенд: [регрессионный стенд](docs/windows-lab.md). Текущие границы расширенного адаптера: [Windows frameworks, 20.09.2026](docs/windows-frameworks-2026-09-20.md). Исторические исследования остаются в `docs` и описывают свои версии, а не готовность текущего релиза.

openWakeWord имеет лицензию Apache-2.0, готовые wake-word веса — CC BY-NC-SA 4.0; они предназначены здесь для личного некоммерческого эксперимента. У Piper, его зависимостей и модели Denis отдельные лицензии; notices и model card включаются в runtime-ресурсы.

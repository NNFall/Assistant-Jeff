// User-selected provisional thresholds for experimental desktop/UI automation.
// Neither value guarantees accuracy or replaces observation/execution guards.
// The main intent provider has a separate policy and does not use this module.
export const MIN_PROBABILITY = 0.8;
export const MIN_CONFIDENCE = 0.8;
